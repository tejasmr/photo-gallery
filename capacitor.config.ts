import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tezzio.photogallery',
  appName: 'photo-gallery',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
